package cf.rachlinski.cryspscript.prerun.parsing.operation;

/**
 * Represents a decimal value stored in base 10, instead of floating point. <br>
 * Theoretically infinite precision
 */
public class DecimalValue
{
	private byte[] whole;
	private byte[] dec;
	private boolean isPositive;



	private static byte[] toBytes(int value)
	{
		byte[] tmp = new byte[4];
		for(int i = 0; i < tmp.length; i++)
		{
			Integer.
		}
	}


	private static byte[] add(byte[] left, byte[] right)
	{

	}
}
